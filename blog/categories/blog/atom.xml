<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Blog | Dark Matter in Cyberspace]]></title>
  <link href="http://leetschau.github.io/blog/categories/blog/atom.xml" rel="self"/>
  <link href="http://leetschau.github.io/"/>
  <updated>2015-07-15T11:08:23+08:00</updated>
  <id>http://leetschau.github.io/</id>
  <author>
    <name><![CDATA[Li Chao]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Build Technical Blog Website With Octopress]]></title>
    <link href="http://leetschau.github.io/blog/2014/07/10/154816/"/>
    <updated>2014-07-10T15:48:16+08:00</updated>
    <id>http://leetschau.github.io/blog/2014/07/10/154816</id>
    <content type="html"><![CDATA[<h1>Install</h1>

<p>Ref: <a href="http://octopress.org/docs/setup/">Octopress Setup</a>.</p>

<pre><code>curl -L https://get.rvm.io | bash -s stable --ruby
rvm install 1.9.3
rvm use 1.9.3
rvm rubygems latest
curl https://raw.githubusercontent.com/creationix/nvm/v0.13.1/install.sh | bash      // install [nvm](https://github.com/creationix/nvm)
echo "source ~/.nvm/nvm.sh" &gt;&gt; ~/.zshrc
nvm install 0.10
nvm use 0.10
cd ~/apps
git clone git://github.com/imathis/octopress.git octopress
cd octopress
gem install bundler
bundle install
rake install
</code></pre>

<p>Note1: Do NOT use ruby 2.1.1, it can&rsquo;t install bundler, while ruby 2.1.2 is OK.</p>

<p>Note2: Ruby 1.9.3 in pre-installed on Ubuntu 14.04. An &ldquo;Permission denied&rdquo; error occurs when <code>gem install bundler</code>.
  You have to <code>rvm install 2.1.2</code> to build a non-root ruby environment to install bundler.</p>

<p>Note3: <code>bundle install</code> is very slow. If it&rsquo;s stuck when installing some components, cancel it with Ctrl-C and rerun this command.</p>

<h1>Usage</h1>

<h2>Setup</h2>

<ol>
<li><p>Create a new repo named &ldquo;<username>.github.io&rdquo; on <a href="https://github.com/">GitHub</a>;</p></li>
<li><p>Build blog website framework:</p>

<pre><code> rake setup_github_pages
</code></pre>

<p> When asked, provide the url (<a href="https://github.com/">https://github.com/</a><username>/<username>.github.io) of above repo.</p></li>
</ol>


<h2>Configuration</h2>

<p>Modify _config.yml to customize blog&rsquo;s title, subtitle, author and blog descriptions.</p>

<p>Under &ldquo;3rd Party Settings&rdquo;, set &ldquo;github_user&rdquo; as &ldquo;<username>&rdquo;.</p>

<h2>Post new blog</h2>

<p>Run the following command to create a new post, and edit post content:</p>

<pre><code>rake new_post["Hello World"]
vi source/_posts/2014-08-18-hello-world.markdown
</code></pre>

<h2>Preview blog website</h2>

<p>Run the following command to generate new post and preview them:</p>

<pre><code>rake generate
rake watch    // optional
rake preview
</code></pre>

<p>If it complains no javascript rumtime found, run <code>nvm use 0.10</code>, where version number &ldquo;0.10&rdquo; is determined by <code>nvm list</code>. Now preview with url &ldquo;<a href="http://localhost:4000">http://localhost:4000</a>&rdquo;.</p>

<h2>Deploy to Github</h2>

<p>Run generate and deploy tasks:</p>

<pre><code>rake generate
</code></pre>

<p>If it complains no javascript rumtime found, run <code>nvm use 0.10</code>.</p>

<pre><code>rake deploy
</code></pre>

<p>You need provide username (without @gmail.com) and password of your github account.</p>

<p>Sometimes when you encounter an error when <code>rake deploy</code>,
it said there are confliction in your github.io repository,
and you have to resolve the confliction by <code>git pull</code>.</p>

<p>Go to $APP_HOME/octopress/_deploy and run:</p>

<pre><code>git pull origin master
git add -A
git ci -m "merge conflict manually"
</code></pre>

<p>Now go to $APP_HOME/octopress and run <code>rake deploy</code> again.
Your blogs will be synchronized to github.</p>

<p>There are many other <a href="http://www.mzlinux.org/?q=node/415">Static site generators</a>. Some based on Python such as <a href="https://github.com/getpelican/pelican">Pelican</a> looks lighter than Octopress.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[自动将Evernote笔记发布到博客上]]></title>
    <link href="http://leetschau.github.io/blog/2013/04/06/000709/"/>
    <updated>2013-04-06T00:07:09+08:00</updated>
    <id>http://leetschau.github.io/blog/2013/04/06/000709</id>
    <content type="html"><![CDATA[<p>Evernote笔记要自动分享到博客还真不那么容易，借鉴<a href="https://arjw.wordpress.com/2011/08/25/of-the-note/#">Using Evernote to Post to WordPress</a>，申请了一个Wordpress帐号，点击dashboard左边的“控制板” -> My Blogs -> Post by Email下面的"Enable"按钮，会出现一个email地址，向这个地址发送邮件就可以发布博客了，可以先将写好的笔记保存在一个文件里，然后通过python的email模块向这个地址发邮件的方法实现自动发布博客，然后用evernote的local api将这个文件变成一个新笔记保存在数据库中。</p>

<p>对于现有的笔记，只能在菜单“笔记->分享->通过电子邮件发送”，填上Email地址的方法，刚才试验成功，但如果修改这个笔记重新发送一次，wordpress上不会覆盖原来的笔记，而是发一条新博文，还有一个问题是wordpress官网的<a href="http://wordpress.com">免费博客</a>虽然很好，但被GFW屏蔽了。</p>

<p><a href="http://aaditya.info/blog/2012/08/clipboard-to-email-python-code/">Clipboard to Email – Python Code</a>演示了如果将剪贴板里的内容通过email发送出去，也是一种解决问题的方法。</p>

<p>python 2.7.3 doc: <a href="http://docs.python.org/2/library/email-examples.html">18.1.11. email: Examples</a>有发送html文档的完整例子；</p>

<p>126和163也支持邮件发博客，用自己的邮箱发送到blogmail@188.com，可以用python的smtplib，通过smtp.163.com发送；</p>

<p>下面这个发送text邮件的实例在163博客上成功发布（sendmail方法如果返回一个空字典，说明发送成功）：</p>

<p> import smtplib
 # Import the email modules we&rsquo;ll need
 from email.mime.text import MIMEText
 # Create a text/plain message
 msg = MIMEText(&lsquo;this is a test blog from email&rsquo;)
 me = &lsquo;<a href="&#109;&#97;&#x69;&#x6c;&#x74;&#x6f;&#58;&#108;&#x65;&#101;&#x5f;&#x63;&#104;&#x61;&#117;&#x40;&#x31;&#x36;&#51;&#46;&#99;&#111;&#109;">&#108;&#x65;&#x65;&#95;&#99;&#104;&#x61;&#x75;&#64;&#x31;&#x36;&#x33;&#x2e;&#99;&#111;&#x6d;</a>&rsquo;
 you = &lsquo;<a href="&#x6d;&#97;&#105;&#x6c;&#116;&#x6f;&#x3a;&#98;&#x6c;&#x6f;&#103;&#x6d;&#x61;&#105;&#x6c;&#x40;&#x31;&#x38;&#x38;&#46;&#x63;&#x6f;&#x6d;">&#x62;&#108;&#111;&#x67;&#x6d;&#x61;&#x69;&#108;&#64;&#x31;&#x38;&#x38;&#46;&#99;&#x6f;&#109;</a>&rsquo;
 msg[&lsquo;Subject&rsquo;] = &lsquo;A Test Blog from Email&rsquo;
 msg[&lsquo;From&rsquo;] = me
 msg[&lsquo;To&rsquo;] = you
 # Send the message via our own SMTP server, but don&rsquo;t include the
 # envelope header.
 s = smtplib.SMTP(&lsquo;smtp.163.com&rsquo;)
 s.login(&lsquo;lee_chau&rsquo;, &lsquo;abaqus67&rsquo;)
 s.sendmail(me, [you], msg.as_string())
 s.quit()</p>

<hr />

<p>2013-04-13更新：</p>

<p>参考<a href="http://fun.blog.ustc.edu.cn/?p=148">从Evernote自动发博客到WordPress</a>，可以用<a href="https://eatags.com/">eatags.com</a>将笔记发布到wordpress.com上，设置好共享之后，要发布的笔记只要加上标签“ eat.wordpress.post ”（参考<a href="https://eatags.com/features%EF%BC%89%E5%B0%B1%E4%BC%9A%E8%A2%AB%E8%87%AA%E5%8A%A8%E5%90%8C%E6%AD%A5%E5%88%B0wordpress.com%E4%B8%8A%EF%BC%8C%E4%BB%8A%E5%A4%A9%E6%B5%8B%E8%AF%95%E7%9A%84%E7%BB%93%E6%9E%9C%E5%8F%AF%E4%BB%A5%E6%98%BE%E7%A4%BA%E5%9B%BE%E7%89%87%EF%BC%8C%E6%98%AF%E7%9B%AE%E5%89%8D%E6%89%80%E7%9F%A5%E6%9C%80%E5%A5%BD%E7%9A%84%E5%90%8C%E6%AD%A5%E6%96%B9%E5%BC%8F%EF%BC%8C%E6%88%96%E8%80%85%E7%94%A8">https://eatags.com/features%EF%BC%89%E5%B0%B1%E4%BC%9A%E8%A2%AB%E8%87%AA%E5%8A%A8%E5%90%8C%E6%AD%A5%E5%88%B0wordpress.com%E4%B8%8A%EF%BC%8C%E4%BB%8A%E5%A4%A9%E6%B5%8B%E8%AF%95%E7%9A%84%E7%BB%93%E6%9E%9C%E5%8F%AF%E4%BB%A5%E6%98%BE%E7%A4%BA%E5%9B%BE%E7%89%87%EF%BC%8C%E6%98%AF%E7%9B%AE%E5%89%8D%E6%89%80%E7%9F%A5%E6%9C%80%E5%A5%BD%E7%9A%84%E5%90%8C%E6%AD%A5%E6%96%B9%E5%BC%8F%EF%BC%8C%E6%88%96%E8%80%85%E7%94%A8</a><a href="http://www.evreblog.us">everblog.us</a>也能将evernote笔记发布到everblog.us网站上，但不能显示图片，everblog选中一个笔记本后会将该笔记本下所有的笔记都发布到everblog网站上，适合批量发布；</p>
]]></content>
  </entry>
  
</feed>
